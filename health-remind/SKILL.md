---
name: health-remind
description: 用于根据用户关于吃药、服药、用药提醒的明确请求，生成单个吃药提醒任务命令并提取可映射为周期性 cron 的执行时间；适用于“提醒我吃药”“设置服药提醒”等场景，不适用于药品咨询、剂量判断、疗程建议、医疗诊断等其他场景；
triggers: 提醒我吃药、设置吃药提醒、设置用药提醒、帮我定个服药提醒
---



# 吃药提醒
用于根据用户输入生成吃药提醒任务命令。当用户明确表达“需要提醒自己吃药 / 服药 / 用药”时，应调用本技能。



## 核心执行规则（必须严格遵循）

### 1. 需求分析
- 忽略用户历史的上下文中的需求。永远只基于**用户最新输入**分析和拆解用户需求，判断**要不要调用工具**。
- 用户需求都要处理，不要遗漏。

### 2. 能力边界
本技能只负责：
- 识别“吃药提醒”意图
- 提取提醒时间
- 将可映射的周期性时间转换为 cron 表达式
- 生成吃药提醒命令

### 3. Cron 表达式规则
`execTime` 使用标准 5 段 cron 表达式，格式如下：

分 时 日 月 周

#### 示例：
- `0 8 * * *` 表示每天 08:00 执行
- `30 20 * * *` 表示每天 20:30 执行
- `0 9 * * 1-5` 表示每周一到周五 09:00 执行
- 若当前日期为 `2026-04-20`，“明天8点提醒我吃药” -> `0 8 21 4 *`

#### 生成规则如下：
- “每天X点” -> 生成每天执行的 cron 表达式
- “每周X的X点” -> 生成按周执行的 cron 表达式
- “工作日X点” -> 生成周一到周五执行的 cron 表达式
- 当用户输入中包含相对日期，如“今天”“明天”“后天”，且输入中提供“当前日期”时，必须先基于当前日期将相对日期解析为具体的月、日，再生成 cron 表达式
- 当用户输入中包含明确日期和时间，或可由“当前日期 + 相对日期”解析出明确日期和时间时，在当前系统仅支持 5 段 cron 表达式的前提下，按“每年该月该日该时刻重复执行”生成 cron 表达式
- 不允许臆造时间
- 不允许输出非 5 段 cron 表达式
- 不允许输出解释性文本到命令中

#### 吃药提醒的模糊时间映射规则如下：
- “早上提醒我吃药” -> 默认按上午 8 点处理 -> `0 8 * * *`
- “中午提醒我吃药” -> 默认按中午 12 点处理 -> `0 12 * * *`
- “下午提醒我吃药” -> 默认按下午 2 点处理 -> `0 14 * * *`
- “晚上提醒我吃药” -> 默认按晚上 18 点处理 -> `0 18 * * *`
- 用户未指定任何时间信息时，不做默认补全，`execTime` 输出空字符串 `""`



## 输入参数

| 字段名      | 类型     | 必填 | 默认值 | 描述                                                |
|----------|--------|----|---|---------------------------------------------------|
| execTime | string | no | — | cron表达式字符串：获取描述中的任务执行时间信息生成cron表达式；如果没有时间信息则该字段为空 |



## 输出命令

示例：
```bash
cd <skill_root> && python scripts/python/health_remind.py --execTime "0 15 * * *"
```
```bash
cd <skill_root> && python scripts/python/health_remind.py --execTime ""
```

>  说明：
> - `cd <skill_root> && python scripts/python/health_remind.py` 为前面固定输出内容，后面携带参数；
> - `<skill_root>` 为技能根目录占位符，运行时替换；
> - 若无法提取出明确且可映射的周期性 cron 时间，则 --execTime 传空字符串。
> - 仅输出 bash 命令即可



## 注意事项
- 只要用户核心诉求是“提醒吃药 / 服药 / 用药”，即可使用本技能。
- 时间信息优先从用户当前输入中提取，不要依赖猜测。
- 对“早上 / 中午 / 下午 / 晚上”这类模糊时间，按本技能预设时间映射处理。
- 对完全未给出时间的提醒请求，不要默认补具体时间，必须输出空 execTime。